---
name: http-testing
description: Use when the user wants to interactively test Application REST API endpoints with curl against a locally-running Spring Boot server, walk through happy-path API sessions with paused review after each call, save responses to src/test/resources/http/response-samples/, or extract Firebase ID tokens from sign-in responses. Triggers on requests to "test the API", "run through the endpoints", or "exercise a user flow against localhost".
---

Read [ai/workflows/http-testing.md](../../../ai/workflows/http-testing.md) and follow it precisely.
